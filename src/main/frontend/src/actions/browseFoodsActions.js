export const applySearchAction = (currentSearch) => ({
    type: 'APPLY_SEARCH',
    currentSearch
})

export const applyCategoryAction = (categories) => ({
    type: 'APPLY_CATEGORY',
    categories
})